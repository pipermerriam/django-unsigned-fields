============
Installation
============

At the command line::

    $ easy_install django-unsigned-fields

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-unsigned-fields
    $ pip install django-unsigned-fields
